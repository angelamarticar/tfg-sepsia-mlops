[build-system]
requires = ["setuptools"]
requires-python = ">=3.10"
build-backend = "setuptools.build_meta"

[project]
name = "sepsis-mlops"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["src"]
